# 流量分析

### 明文flag

1. `ctrl + F `
2. 选 `分组字节流`+`字符串`
3. 搜 `flag{`

### 编码flag

1. flag(ASCII)的hex编码：  666c6167
2. 搜索同上

### wireshark规则

`http.request or http.response.code == 200`

`http.request.method == POST`

1. 搜关键词
2. binwalk 整个流量文件，看能不能分离有用的东西
